Tasks to be assigned for submission 1:

1. Source website profile: Create a web page containing all available profile information about a source website, as well as the last 10 articles of that page. This profile page must be hyperlinked with the source name from the results on the main search page.
2. Word stats: For a search query (one or multiple keywords), display word-level statistics for the 50 latest articles (based on availability), counting all unique words in descending order (by frequency of the words). This statistics page must be hyperlinked from the search terms above the results. You must use the Java 8+ Streams API to process the article’s description field from the news API.
3. News Sources: Add a button to the home page, and display the news sources available, which can be filtered based on the following criteria: country, category (e.g., sports, science, etc. ), or language.
4. Article Sentiment: Given a stream of (up to 50) articles, determine if their descriptions are overall happy ’:-)’, sad ’:-(’ or neutral ’:-|’. This sentiment will be displayed on the main search page for each search query (see Figure 2). Process the stream of articles, finding happy and sad words, emojis, and emoticons, and counting them (create your own “happy/sad” lists). If a submission contains more than 70% “happy” strings, return a happy sentiment :-), for more than 70% sad, return an unhappy emoticon :-(, otherwise a neutral one :-|. For a stream of articles, calculate the average of the individual articles’ results. You must use the Java 8 Streams API to process the submissions.
5. Description readability: Given a stream of (up to 50) articles, calculate the description’s readability using Flesh-Kincaid Grade Level and the  Flesch Reading Score (see Figure 2). Also, calculate the readability average of all the articles’ descriptions listed in the search terms. You must use the Java 8+ Streams API to process the results. 

Alex - 40321783 - Task #

Nirvana - 40278300 - Task # 3: News Sources

Mustafa - xxx - Task #

Khashayar - xxx - Task #

Zahra - xxx - Task #

